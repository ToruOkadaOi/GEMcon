model_config:
  objective_reaction: "r_biom_out"

output:
  models_dir: "./models"

data_dir: "data/data_processed"

gimme:
  solver: "GLPK"

tinit:
  solver: "GLPK"
  essential_reactions:
    - "r_biom_out"
    - "r_hex"
    - "r_atpase"

corda:
  solver: "glpk"

fastcore:
  solver: "glpk"

imat:
  solver: "GLPK"